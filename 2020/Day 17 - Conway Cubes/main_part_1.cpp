#include <bits/stdc++.h>

using namespace std;

// set up input
ifstream file("input.txt");
string line;

// variables

void readInput() {
    while (getline(file, line)) {
        //
    }
}

int main(void) {
    // read input
    readInput();
    return 0;
}